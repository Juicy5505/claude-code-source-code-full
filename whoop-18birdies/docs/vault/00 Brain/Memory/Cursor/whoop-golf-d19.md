# Cursor memory — WHOOP Golf D19

- Dual maximize: Apple Watch (trail-right, live) + WHOOP 5 delayed import / readiness — one icon `com.alex.whoopgolf`.
- **Both-required admission:** new rounds need Watch live-capture proven **and** WHOOP swing source proven (`DualWearableRequirement`). Single-wearable modes are diagnostic only.
- Shot yards = phone GPS between verified swings; F/M/B overlay only from licensed hole geometry.
- Never hang on WHOOP live `TOGGLE_IMU` / Arming for golf.
- Vault App Overview: `10 Projects/Whoop Golf Companion/App Overview.md` (sanitized only — no tokens, UDIDs, team IDs, IPs, raw HR).
- Graphify cadence: after each wave `graphify update .` in `whoop-18birdies/apple`; vault update when mounted; append why-notes — do not rewrite D15 / D18.
- Watch OS gate **CLEARED** (2026-08-21 Alex confirm). Physical WhoopGolfWatch install from `apple/WhoopGolf.xcodeproj` is unblocked — runbook `apple/docs/MAC-WATCH-INSTALL.md`. Do not claim on-device install from cloud.
